[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/opt/hermes/docker/railway-start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
